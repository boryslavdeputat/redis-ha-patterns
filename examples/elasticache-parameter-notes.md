# ElastiCache notes

- `reserved-memory-percent` for failover headroom
- enable Multi-AZ + auto failover for replication groups
- in-transit encryption for sensitive data
- parameter groups version-pinned per env
